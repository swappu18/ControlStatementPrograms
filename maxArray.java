public class maxArray {
    public static void main(String[] args) {
        int [] arr=new int{200,125,10,23,500}
        int max=arr[0];
        for (int i=0;i<array.length;i++)
        {
            if(array[i])>max)
            max=array[i];

        }
        System.out.println("largest element in array is in given array"+max);

    }
}
